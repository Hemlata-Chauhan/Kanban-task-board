# Kanban-task-board
I built a Kanban board using React and Vite, managed application state using useState, passed data via props, and implemented task creation, deletion, and movement between columns following React’s unidirectional data flow.
